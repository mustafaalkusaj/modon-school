# نظرة عامة على النظام — System Overview

## فكرة النظام الأساسية

منصة إدارة مدرسية متكاملة لـ:
- إدارة الطلاب
- متابعة الحسابات والدفعات
- تسجيل الحضور
- إدارة الرواتب والمصروفات
- إنشاء التقارير المالية

**اللغات المدعومة:** عربي (RTL) + English (LTR)

---

## البنية التنظيمية

### المستويات الهرمية

```
Super Admin (المدير العام)
  ↓
Schools (المدارس)
  ↓
Branches (الفروع)
  ↓
Users (مديرو الفروع)
  ↓
Students (الطلاب)
```

### المدارس والفروع

**المدرسة (School):**
- الوحدة الكبرى
- قد تحتوي على عدة فروع
- لها هويتها الخاصة (شعار، ألوان، اسم)
- مثال: مدرسة الكرامة

**الفرع (Branch):**
- تابع للمدرسة
- موقع جغرافي منفصل
- له بيانات مالية منفصلة
- مثال: فرع بغداد - فرع البصرة

**Scope Enforcement:**
- Admin الفرع: يرى بيانات فرعه فقط
- Admin المدرسة: يرى جميع فروع مدرسته
- Super Admin: يرى كل المدارس والفروع

---

## الأدوار والصلاحيات

### الأدوار الرئيسية

#### 1. Super Admin
- إدارة جميع المدارس والفروع
- إدارة المستخدمين
- متابعة الاشتراكات
- صفحات فقط له:
  - `/ar/super-admin`
  - `/ar/group` (إدارة المدارس)
  - `/ar/monitoring` (مراقبة النظام)
  - `/ar/subscriptions`

#### 2. Admin (School/Branch Admin)
- إدارة الطلاب (إضافة، تعديل، حذف، نقل، توقيف)
- تسجيل الدفعات والمدفوعات
- تسجيل الحضور
- عرض التقارير
- إدارة الرواتب والمصروفات
- صلاحيات محددة:
  - `edit_students` - تعديل بيانات الطالب
  - `delete_students` - حذف الطالب
  - `create_payments` - تسجيل دفعات
  - `view_attendance` - عرض الحضور
  - `view_reports` - عرض التقارير

#### 3. Employee
- تسجيل الدفعات فقط
- عرض بيانات الطلاب
- عرض الحضور
- صلاحيات محدودة جداً

---

## إدارة الطلاب

### حالات الطالب (Student Status)

| الحالة | المعنى | الأزرار المتاحة |
|--------|--------|-----------------|
| **Active** | طالب نشط | نقل، توقيف، حذف |
| **Suspended** | طالب موقوف | إعادة تفعيل، نقل، حذف |
| **Transferred** | طالب منقول | إعادة تفعيل، توقيف، حذف |
| **Deleted** | طالب محذوف | استعادة، حذف نهائي |

### عمليات على الطالب

#### إضافة طالب
```
الحقول المطلوبة:
- الاسم الكامل
- الصف
- الشعبة (اختياري)
- العنوان
- الهاتف (اختياري)
- إجمالي الرسوم
```

#### استيراد طلاب (Bulk Import)
```
Format: Excel (.xlsx)
الأعمدة:
- اسم الطالب (اسم الطالب / Student name / Name)
- الصف (الصف / Class)
- الشعبة (الشعبة / Section)
- الهاتف (الهاتف / Phone)
- العنوان (العنوان / Address)
- إجمالي الرسوم (إجمالي الرسوم / Total fees)

المراحل:
1. رفع الملف → Parse
2. معاينة الأخطاء → Validate
3. تأكيد الاستيراد → Import
```

#### نقل الطالب (Transfer)
```
الخيارات:
- نقل الصف (تغيير الصف فقط)
- نقل الشعبة (تغيير الشعبة فقط)
- نقل كامل (transferred status)
```

#### توقيف الطالب (Suspend)
```
التأثير:
- Status → suspended
- يظهر في tab "الموقوفون"
- لا يحذف من النظام
- يمكن إعادة تفعيله
```

#### حذف الطالب (Delete)
```
Soft Delete:
- Status → deleted
- يظهر في tab "المحذوفون"
- يمكن استعادته

Hard Delete (بعد soft delete):
- حذف نهائي
- لا يمكن استعادته
```

---

## الحسابات والدفعات (Payments)

### العلاقة: الطالب → القسط → المدفوع → المتبقي

```
مثال:
طالب: أحمد علي
- إجمالي القسط: 500 دينار
- المدفوع: 200 دينار
- الخصم: 50 دينار
- المتبقي: 250 دينار (500 - 200)
- الحالة: partially_paid

صيغة:
remaining = max(total - paid, 0)
status = 'fully_paid' إذا remaining <= 0 و total > 0
status = 'partially_paid' إذا paid > 0 و remaining > 0
status = 'unpaid' إذا paid = 0 و total > 0
status = 'no_fee_configured' إذا total = 0
```

### تسجيل دفعة
```
البيانات المطلوبة:
- الطالب (اختيار من البحث)
- المبلغ
- تاريخ الدفعة
- طريقة الدفع (نقداً / تحويل بنكي / شيك)
- رقم الإيصال الورقي (اختياري)
- رقم الإيصال الإلكتروني (اختياري)
- ملاحظات (اختياري)
```

### التأثير على البيانات
```
عند تسجيل دفعة:
- paid_fee للطالب ← يزيد
- remaining_fee ← ينخفض (محسوب تلقائياً)
- Status المالي ← قد يتغير
- لوحة الفرع ← تحدث تلقائياً
```

---

## الحضور (Attendance)

### تسجيل الحضور
```
البيانات:
- التاريخ
- الطلاب النشطون يظهرون تلقائياً
- لكل طالب: حاضر / غائب / متأخر / بعذر

التأثير:
- يتم الحفظ في جدول attendance_records
- يُستخدم في التقارير
```

### سجل الحضور للطالب
```
يعرض:
- آخر 14 يوم من الحضور
- إحصائيات: عدد الحضور، الغياب، التأخر
- نسبة الحضور
```

---

## الاستيراد والتصدير

### استيراد الطلاب (Import)
```
المراحل:
1. Parse: قراءة الملف، تحديد الأعمدة
2. Validate: التحقق من البيانات، تقرير الأخطاء
3. Import: إدراج الطلاب، ربط المعلمين تلقائياً

أمان:
- النظام لا يُرسل generated columns (remaining_fee, paid_fee, etc.)
- يتحقق من الصف والشعبة قبل الإدراج
- يحافظ على school_id و branch_id
```

### تصدير الطلاب (Export)
```
يصدر:
- الطلاب النشطين فقط أو حسب التبويب
- الأسماء، الصفوف، الشعب، الهواتف، العناوين
- البيانات المالية (القسط، المدفوع، المتبقي)
- الحالة

الصيغ:
- Excel (.xlsx) - الصيغة الوحيدة المدعومة
```

---

## لوحة الفرع (Branch Dashboard)

### البيانات المعروضة

```
إحصائيات مالية:
- إجمالي الرسوم (مجموع جميع أقساط الطلاب النشطين)
- المدفوع (مجموع جميع الدفعات)
- المتبقي (الفرق بينهما)

التحديث:
- يحدث تلقائياً عند:
  - إضافة طالب
  - تعديل قسط
  - تسجيل دفعة
  - حذف طالب
  - تغيير حالة الطالب

مصدر البيانات:
- API: /api/web/payments/overview
- نفس الحسابات كـ Payments page
- guaranteed consistent
```

---

## التقارير

### أنواع التقارير

#### 1. تقرير الطلاب
```
يحتوي:
- بيانات الطلاب
- الأقساط المخصصة لهم
- الحالة المالية
```

#### 2. تقرير الحسابات
```
يحتوي:
- سجل الدفعات
- التحصيلات
- المبالغ المتأخرة
```

#### 3. الملخص المالي
```
يحتوي:
- إجمالي الرسوم
- إجمالي المدفوع
- إجمالي المتبقي
- إجمالي الخصومات
```

---

## الصفحات الرئيسية

| الصفحة | المسار | الوصف |
|--------|--------|--------|
| لوحة التحكم | `/ar/dashboard` | ملخص مالي سريع |
| الطلاب | `/ar/students` | إدارة الطلاب |
| الحسابات | `/ar/payments` | تسجيل ومتابعة الدفعات |
| الحضور | `/ar/attendance` | تسجيل الحضور اليومي |
| الرواتب | `/ar/salaries` | إدارة رواتب المعلمين |
| المصروفات | `/ar/expenses` | تسجيل مصروفات المدرسة |
| التقارير | `/ar/reports` | إنشاء التقارير |
| لوحة الفرع | `/ar/branch-overview` | ملخص الفرع |

---

## تكامل النظام

```
الطالب ← الصف/الشعبة ← class_fees (القسط)
  ↓
payments (الدفعات)
  ↓
financial summary (الملخص المالي)
  ↓
branch dashboard + reports

attendance (الحضور)
  ↓
attendance records
  ↓
reports

salaries (الرواتب)
  ↓
payroll
  ↓
reports

expenses (المصروفات)
  ↓
expense records
  ↓
reports
```

---

## قاعدة البيانات (PostgreSQL)

### الجداول الرئيسية

| الجدول | الغرض |
|--------|--------|
| schools | المدارس |
| branches | الفروع |
| students | الطلاب |
| class_fees | أقساط الصفوف |
| payments | السجلات المالية |
| attendance_records | سجل الحضور |
| profiles | بيانات المستخدمين |
| salaries | الرواتب |
| expenses | المصروفات |

### العلاقات
```
schools ← branches ← students ← class_fees
                        ↓
                     payments
                        ↓
                   attendance_records
```

---

## الأمان والصلاحيات

### الحماية على مستويات

1. **Route Level:** Protected routes تحتاج تسجيل دخول
2. **API Level:** كل endpoint يتحقق من الصلاحيات
3. **Query Level:** حيث WHERE clauses تفلترت بـ school_id و branch_id
4. **Database Level:** RLS policies على الجداول

### Scope Isolation

```
Super Admin: يرى كل المدارس
Admin: يرى مدرسته وفروعها فقط
Branch Admin: يرى فرعه فقط
```

---

## الأداء والحدود

### الحدود الموصى بها

- عدد الطلاب في الفرع: 5,000+ (مع pagination)
- عدد السجلات المالية: unlimited (indexed)
- الحد الأقصى لحجم الملف في الاستيراد: 5MB

### Pagination

- الجداول الكبيرة تُعرض بـ 50 صف/صفحة
- البحث يستخدم debounce بـ 180ms
- الفلاترة محلية (client-side)

---

## البيئات

```
Development: localhost:3000
Staging: Not configured
Production: https://modon-school.com

Database: Supabase PostgreSQL
Hosting: Vercel
Auth: JWT + Session Cookies
```
