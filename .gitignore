# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# legacy project directories
/00990090
/school-acc-system

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/output/
artifacts/reliability-audit/*storage-state.json

# next.js
/.next/
/out/
.next_tmp_build_*/
.next_bak_*/
# deploy staging dir (NEXT_DIST_DIR) and the build it displaces
/.next-build/
/.next.previous/
# throwaway NEXT_DIST_DIR targets used to verify build flags locally
/.next-nocache/
/.next-cputest/

# production
/build

# misc
.DS_Store
*.pem
.claude/
.augment/
.continue/
.vscode/
*.code-workspace
.playwright-cli/
supabase/.temp/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.production.example
.env.e2e.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.env*.local

.env.production.local
.env.local
.env.e2e.local
# Added by code-review-graph
.code-review-graph/

audit/
logs/

# leftover scripts / backups (do not rsync to prod)
.tmp-*
.env.bak*
*.bak-fra-*
/.next-cputest/

# Bulk-provisioning output: usernames + passwords for real students.
# Never commit, never rsync to a server.
student-credentials-*.txt
*-credentials-*.txt

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
