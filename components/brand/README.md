# Brand Components

This folder groups the visual branding pieces used across the web admin.

- `SchoolLogo.tsx`: school logo image with safe fallback initial.
- `BrandLockup.tsx`: logo + text lockup used in headers, login, and sidebars.
- `index.ts`: central exports for brand UI primitives.
