/**
 * Shared scoped styles for school module pages.
 * All layout, sidebar, topbar, table, and modal styles now live in
 * app/[locale]/globals.css via CSS custom properties.
 * This file is kept as a minimal reset only.
 */
export const SCHOOL_MODULE_CSS = `
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
`;
